{
  "name": "@repo/typescript-config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript compiler presets for Workers apps, React/Vite apps, and internal libraries.",
  "keywords": [
    "react",
    "shared-config",
    "tsconfig",
    "typescript",
    "vite",
    "workers"
  ],
  "license": "MIT",
  "sideEffects": false,
  "exports": {
    "./library.json": "./library.json",
    "./strict.json": "./strict.json",
    "./workers.json": "./workers.json",
    "./vite-react.json": "./vite-react.json",
    "./vite-node.json": "./vite-node.json"
  },
  "scripts": {
    "format": "pnpm -w exec oxfmt --check packages/typescript-config",
    "format:fix": "pnpm -w exec oxfmt packages/typescript-config",
    "lint": "pnpm -w exec cross-env SYNCKIT_TIMEOUT=120000 oxlint --no-error-on-unmatched-pattern packages/typescript-config",
    "lint:fix": "pnpm -w exec cross-env SYNCKIT_TIMEOUT=120000 oxlint --no-error-on-unmatched-pattern --fix packages/typescript-config"
  },
  "monorepo": {
    "kind": "config",
    "role": "typescript-config",
    "agentGuide": "AGENTS.md"
  }
}
