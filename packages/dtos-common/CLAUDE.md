@AGENTS.md

## Claude Code

- Schema changes are API contract changes - update `worker-api` and `front-app` in the same PR.
- Contract authoring rules load from `.claude/rules/contracts.md` when editing `src/**`.
