---
"front-app": patch
---

Fix React Doctor maintainability and accessibility warnings: hoist static variant maps to module scope in `Button` and `Card`, and drop a redundant `role="img"` on logo images.
