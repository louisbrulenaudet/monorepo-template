{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "envMode": "strict",
  "globalDependencies": [
    "biome.json",
    "tsconfig.json",
    "pnpm-workspace.yaml",
    ".npmrc"
  ],
  "globalPassThroughEnv": ["CI", "NODE_ENV"],
  "concurrency": "20",
  "tasks": {
    "lint": {
      "cache": false,
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": []
    },
    "format": {
      "cache": false,
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": []
    },
    "check": {
      "cache": false,
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": []
    },
    "check-types": {
      "cache": false,
      "dependsOn": ["^check-types", "types"],
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/tsconfig.json"],
      "outputs": []
    },
    "types": {
      "cache": false,
      "dependsOn": ["^types"],
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": ["worker-configuration.d.ts"]
    },
    "build": {
      "cache": false,
      "dependsOn": ["^build", "check-types", "types"],
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": ["dist/**"],
      "env": ["NODE_ENV", "VITE_API_BASE_URL"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "passThroughEnv": ["VITE_API_BASE_URL"]
    },
    "preview": {
      "dependsOn": ["build"],
      "cache": false,
      "persistent": true,
      "passThroughEnv": ["VITE_API_BASE_URL", "NODE_ENV"]
    },
    "deploy": {
      "dependsOn": ["^build"],
      "cache": false,
      "outputLogs": "new-only",
      "passThroughEnv": [
        "CI",
        "NODE_ENV",
        "CLOUDFLARE_API_TOKEN",
        "CLOUDFLARE_ACCOUNT_ID",
        "CF_ACCOUNT_ID"
      ]
    }
  }
}
