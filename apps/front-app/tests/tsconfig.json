{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": ["../tsconfig.app.json", "@repo/typescript-config/tests.json"],
  "compilerOptions": {
    "types": ["vite/client"]
  },
  "include": [
    "${configDir}/**/*.ts",
    "${configDir}/**/*.tsx",
    "${configDir}/../src/**/*.ts",
    "${configDir}/../src/**/*.tsx",
    "${configDir}/../vitest.setup.ts"
  ]
}
