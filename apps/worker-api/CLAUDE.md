@AGENTS.md

## Claude Code

- HTTP contract changes require `packages/dtos-common`, `worker-api`, and `front-app` in the same PR.
- Hono gateway patterns load from `.claude/rules/backend/hono-gateway.md` when editing `src/**`.
